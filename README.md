# thunderbird_archives_send
This script monitors a directory and detects changes within files; using Thunderbird (configured by the user), it drafts an email to the recipient listing the modified files, attaches them, and sends the message. It can be used to monitor a directory containing frequently modified documents and send them out regularly at user-defined intervals.
